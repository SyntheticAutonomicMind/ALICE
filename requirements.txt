# ALICE Dependencies
# SPDX-License-Identifier: GPL-3.0-only
#
# Updated for ROCm 10.0.0 + PyTorch 2.13.0
# Uses >= constraints to install the latest compatible versions.
#
# IMPORTANT: torch, torchvision, and torchaudio are NOT listed here because they
# require platform-specific installation (ROCm on AMD, CUDA on NVIDIA,
# CPU-only, etc.).  Install scripts handle PyTorch installation separately.
# The ROCm 10.0.0 multi-arch packages are installed via the stable.repo.amd.com
# index with device-* extras that cover gfx1103, gfx1151, and all other
# supported AMD GPU architectures.
#
# setuptools<81 is pinned because setuptools 81+ removed pkg_resources, which
# is still needed at build time by some packages (e.g., pandas via
# stable-audio-tools) on Python slim images where it was never bundled.

# FastAPI and server
fastapi>=0.104.1
uvicorn[standard]>=0.24.0
pydantic>=2.12.5
pydantic-settings>=2.1.0

# Diffusion models
# diffusers: use the latest git main so MiniMax-Music3 and other latest
# pipelines are importable.  Pinned releases may be too old.
git+https://github.com/huggingface/diffusers
transformers>=5.0
accelerate>=1.12.0
safetensors>=0.7.0
huggingface-hub>=1.0.0

# Prompt weighting and long prompts
compel>=2.3.1

# Image processing
pillow>=12.0.0

# Configuration
pyyaml>=6.0.3
ruamel.yaml>=0.18.0

# Async file handling
aiofiles>=23.2.1

# Async HTTP client (for download manager)
# 3.9.x has no pre-built wheel for Python 3.13 - use 3.11+ for binary wheels
aiohttp>=3.9.1

# Multipart form data (for file uploads)
python-multipart>=0.0.6

# System monitoring
psutil>=7.1.3

# Audio generation (Stable Audio Open 1.0 and compatible diffusion-audio models)
# torchaudio is installed alongside torch by the install scripts (platform-specific).
# No version pins: these packages must coexist with the platform-specific
# PyTorch stack (ROCm on AMD, CUDA on NVIDIA, CPU on macOS).
einops
# Override pandas to a version with Python 3.13 wheels.
# stable-audio-tools pins pandas==2.0.2, which has no Python 3.13 wheel
# and fails to build from source.
pandas>=2.2.3
stable-audio-tools
